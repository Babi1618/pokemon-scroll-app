import { StyledHeader, StyledHeaderTitle } from "./Header.styled"

export const Header =()=>{
    return (<StyledHeader>
        <StyledHeaderTitle>
            Pokemon Scroll - App
        </StyledHeaderTitle>
    </StyledHeader>)
}